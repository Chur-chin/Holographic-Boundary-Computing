import numpy as np
import matplotlib.pyplot as plt
from scipy.optimize import curve_fit

# ---- 기존 함수 재사용 ----
# compute_accuracy(wavelength, theta, L)

theta_fixed = np.pi / 4  # representative polarization
wavelengths = np.linspace(0.5, 5.0, 60)

accuracies = np.array([
    compute_accuracy(wl, theta_fixed, L)
    for wl in wavelengths
])

# ---- fitting (coherence decay model) ----
def fit_func(x, A, alpha, beta):
    return A * np.exp(-alpha * x) + beta

params, _ = curve_fit(fit_func, wavelengths, accuracies, p0=[1, 0.5, 0.2])

# ---- plot ----
plt.figure(figsize=(6,5))

plt.scatter(wavelengths, accuracies, label="Simulation Data")

x_fit = np.linspace(0.5, 5.0, 200)
y_fit = fit_func(x_fit, *params)

plt.plot(x_fit, y_fit, label="Exponential Fit")

plt.xlabel("Wavelength (λ)")
plt.ylabel("XOR Accuracy")
plt.title("Wavelength Dependence of XOR Accuracy")

plt.legend()
plt.grid(True)
plt.tight_layout()
plt.show()

print("Fit parameters:")
print("A =", params[0])
print("alpha =", params[1])
print("beta =", params[2])
